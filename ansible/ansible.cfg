[defaults]
inventory = inventory.yml
interpreter_python = auto_silent
forks = 10
callback_result_format = yaml
retry_files_enabled = False

[ssh_connection]
pipelining = True
ssh_args = -o BatchMode=yes -o ControlMaster=auto -o ControlPersist=60s
