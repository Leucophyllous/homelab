# このファイルをコピーして .env を作り、実際の値を入れること。
# .env は .gitignore で除外されているためコミットされない。

# --- docker-vm: homepage widget用 ---
N8N_API_KEY=

# --- db-vm: mariadb ---
MARIADB_ROOT_PASSWORD=

# 各スタックの env_file が指す .env (compose/<host>/<stack>/.env) にも
# 別途、サービス固有の値(DISCORD_TOKEN, GOOGLE_CLIENT_SECRET 等)を用意すること。
